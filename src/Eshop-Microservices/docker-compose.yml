services:
  catalogDb:
    image: postgres

  catalog.api:
    image: ${DOCKER_REGISTRY-}catalogapi
    build:
      context: .
      dockerfile: Services/Catalog.Api/Dockerfile

  basketDb:
    image: postgres

volumes:
  postgres_catalogDb:
  postgres_basketDb:
